
1. $ mkdir ...

2. $ git init

3. $ git add ...(file name or --all)

4. $ git commit -m "...(comment)"

5. $ git remote origin master ...(http:// remote repository's URL)

6. $ git pull origin master

7. $ git push origin master
